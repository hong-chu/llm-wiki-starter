# Entities

People, organizations, products, papers, datasets, benchmarks. One
entity per file. Use the **Entity template** in
[`../../CLAUDE.md`](../../CLAUDE.md).

## Pages
*(empty — populate as you ingest sources)*
